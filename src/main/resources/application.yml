spring:
  application:
    name: configcat-demo
  profiles:
    active: local
    
server:
  port: 8080

# OpenAPI/Swagger Configuration
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    operationsSorter: method
    tagsSorter: alpha
    docExpansion: none
    defaultModelsExpandDepth: 2
    defaultModelExpandDepth: 2
  show-actuator: false

# Logging configuration
logging:
  level:
    com.example.configcat: INFO
    com.configcat: INFO
    root: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [%X{correlationId:-}] [%X{userId:-}] %logger{36} - %msg%n"

# Management endpoints
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus,configcat
  endpoint:
    health:
      show-details: always
    configcat:
      enabled: true
  info:
    env:
      enabled: true
    java:
      enabled: true
    os:
      enabled: true

# ConfigCat configuration
configcat:
  log-level: INFO
  user-management:
    sdk-key: "YOUR_USER_MANAGEMENT_SDK_KEY"
    poll-interval-seconds: 30
  payment:
    sdk-key: "YOUR_PAYMENT_SDK_KEY"
    poll-interval-seconds: 30

---
spring:
  config:
    activate:
      on-profile: local

configcat:
  log-level: DEBUG
  user-management:
    sdk-key: "PKDVCLf-Hq-h-kCzMp-L7Q/YOUR_USER_MANAGEMENT_SDK_KEY"
    poll-interval-seconds: 10
  payment:
    sdk-key: "PKDVCLf-Hq-h-kCzMp-L7Q/YOUR_PAYMENT_SDK_KEY"
    poll-interval-seconds: 10

---
spring:
  config:
    activate:
      on-profile: development

configcat:
  log-level: INFO
  user-management:
    sdk-key: "PKDVCLf-Hq-h-kCzMp-L7Q/YOUR_DEV_USER_MANAGEMENT_SDK_KEY"
    poll-interval-seconds: 30
  payment:
    sdk-key: "PKDVCLf-Hq-h-kCzMp-L7Q/YOUR_DEV_PAYMENT_SDK_KEY"
    poll-interval-seconds: 30

---
spring:
  config:
    activate:
      on-profile: production

configcat:
  log-level: WARN
  user-management:
    sdk-key: "${CONFIGCAT_USER_MANAGEMENT_SDK_KEY:configcat-sdk-1/9gPeCHC2w0-Kjegr_dwvmQ/Cl_ld6JRLk6Ev-wW1S8KDg}"
    poll-interval-seconds: 60
  payment:
    sdk-key: "${CONFIGCAT_PAYMENT_SDK_KEY:configcat-sdk-1/9gPeCHC2w0-Kjegr_dwvmQ/-X7xzRL9oEuM2PdC4bq0VQ}"
    poll-interval-seconds: 60
